# --- Core agent stack ---
deepagents>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langgraph>=0.2.50

# --- Vector store ---
weaviate-client>=4.9.0,<5.0.0
langchain-weaviate>=0.0.4

# --- Tracing ---
langfuse>=2.50.0

# --- Parsers (format-agnostic ingestion) ---
pymupdf>=1.24.0           # fast PDF text extraction
docling>=2.0.0            # robust PDF + tables + OCR fallback
python-docx>=1.1.0        # DOCX
python-pptx>=1.0.0        # PPTX
trafilatura>=1.12.0       # HTML / web pages
markdown-it-py>=3.0.0     # Markdown
beautifulsoup4>=4.12.0    # HTML fallback
chardet>=5.2.0            # encoding detection for text/csv

# --- Best-of-Best Ingestion Pipeline (v2) ---
unstructured[all-docs]>=0.13.0    # Stage 1: Multi-format parsing + element types
spacy>=3.7.0                       # Stage 2: Sentence segmentation (SemanticChunker)
sentence-transformers>=3.0.0       # Stage 2: Semantic breakpoints (reuses bge-m3)
keybert>=0.8.0                     # Stage 3b: Keyword extraction (reuses bge-m3)
langdetect>=1.0.9                  # Stage 3c: Language detection (55 languages)
datasketch>=1.0.8                  # Stage 4: MinHash LSH near-dedup (100K+ scale)

# --- UI & utilities ---
streamlit>=1.39.0
watchdog>=5.0.0
python-dotenv>=1.0.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
httpx>=0.27.0
tenacity>=9.0.0
tqdm>=4.66.0
